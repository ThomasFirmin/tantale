[package]
name = "tantale_core"
version = "0.1.0"
edition = "2024"
license = "CECILL-C"
readme = "README.md"
rust-version = "1.91.1"
repository = "https://github.com/ThomasFirmin/tantale"
description = "This the core module for Tantale. Tantale is a Rust library for Automated Machine Learning (AutoML) focusing on Hyperparameter Optimization and Neural Architecture Search. It provides a modular and extensible framework for defining search spaces, objectives, and optimization algorithms, with support for distributed computing and parallel execution."
keywords = ["automl", "hpo", "nas"]

[lib]
path = "src/lib.rs"

[dependencies]
num = "0.4.3"
rand = "0.9.0"
rayon = "1.10.0"
csv = "1.1"
serde = {version="1.0.219", features=["derive","rc"]}
rmp-serde = "1.3.0"
mpi = {version = "0.8.0", features=["derive"], optional=true}
bincode = {version = "2.0.1", features=["serde"], optional=true}
bitvec = { version = "1.0.1", features = ["serde"], optional=true }
num_cpus = "1.17.0"
indexmap = "2.13.0"

[features]
mpi = ["dep:mpi","dep:bincode","dep:bitvec"]

[package.metadata.docs.rs]
rustdoc-args = ["--html-in-header", "katex-header.html"]

[dev-dependencies]
tantale = { path = "../", package = "tantale" }
